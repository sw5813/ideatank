<% provide(:title, "Reset Password") %>
<h1>Reset Password</h1>

<p>Forgot your password? No worries! Enter the same email you used to sign up below and we'll send you a link to reset your password.</p><br>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(:password_reset, url: password_resets_path) do |f| %>

			<%= f.label :email %>
			<%= f.text_field :email %>

			<%= f.submit "Reset Password", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>